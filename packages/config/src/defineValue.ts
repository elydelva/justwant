/**
 * @justwant/config — defineValue
 * Defines a value: which source and how to look it up.
 */

import type { ValueDef } from "./types/index.js";

export function defineValue(def: ValueDef): ValueDef {
  return def;
}
