# @justwant/id

## 0.1.0

### Minor Changes

- Initial release. Sortable IDs (by time) and short URL-safe IDs. Simple names: `createId()`, `getTimestamp(id)` in `/sortable`; `createId(size?)` in `/short`. Primitives in `/primitives`. Subpath exports only.
