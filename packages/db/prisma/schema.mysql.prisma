generator client {
  provider = "prisma-client-js"
  output   = "../generated/prisma-mysql"
}

datasource db {
  provider = "mysql"
  url      = env("DATABASE_URL")
}

model User {
  id    String  @id
  email String
  name  String?
}
