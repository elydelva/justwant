generator client {
  provider = "prisma-client-js"
  output   = "../generated/prisma-postgres"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model User {
  id    String  @id
  email String
  name  String?
}
